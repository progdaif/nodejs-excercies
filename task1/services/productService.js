/**
 * Wrap products entities
 * @module productService
 */

/** @const productModel [DB model for products table] */
const productModel = require('../models/product');

module.exports = {
    productModel
}